// String literal types

let autoComplete: "on" | "off" = "on";

console.log(autoComplete);
autoComplete = "off";
console.log(autoComplete);

// WILL OT COMPILE
// autoComplete = "boom";
// console.log(autoComplete);
